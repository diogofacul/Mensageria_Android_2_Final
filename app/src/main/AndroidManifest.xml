<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mensageria">
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_my_icon_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_my_icon_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mensageria"
        tools:targetApi="29">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presenter.RegisterLoginActivity" />
        <activity android:name=".presenter.ContactMenuActivity" />
        <activity android:name=".presenter.SMSRecoveryActivity" />
        <activity android:name=".presenter.ChatActivity" />

    </application>

</manifest>